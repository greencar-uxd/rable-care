# 작업 지침

[출력 원칙]
- 결론부터. 서론·요약 반복·맺음말 인사 금지.
- 묻지 않은 내용 추가 설명 금지. 확장은 요청받을 때만.
- 만들기 전에 판단: 꼭 필요한가 → 이미 있는 걸 재사용 가능한가
  → 표준 기능으로 되는가 → 그래도 필요하면 최소한만.
- 예시는 1개면 충분. 같은 말 다르게 반복 금지.
- 검증·에러 처리·보안·접근성은 축약 대상에서 제외.

[검증 원칙]
- 팩트 / 추론 / 확인 필요를 구분해 표기.
- 외부 사실은 출처 명시. 근거 없으면 없다고 말할 것.
- 불확실하면 단정 대신 가능 시나리오로.

[진행 원칙]
- 매 단계 확인받지 말고 자율 진행. 막힐 때만 질문.

---

## 이 레포 고유 규칙

**구성 (레이블케어 웹사이트 시안)**
- `/` 허브(두 컨셉 미리보기·링크) · `/corporate/` 컨셉1 블루 기업형(정적 6페이지) · `/editorial/` 컨셉2 브론즈 프리미엄(단일 파일 SPA).
- `editorial/`은 `support.js` 런타임 + `vendor/` React 18 UMD 자체 호스팅으로 렌더된다. 시안 수정은 `editorial/index.html`에서만.
- 허브 카드 썸네일은 `assets/preview-*.png`(실제 렌더 캡처) — 시안 화면이 바뀌면 재캡처.

**배포**
- GitHub Pages "Deploy from a branch": 소스는 `claude/design-mockup-comparison-z9e5gw` / root. 이 브랜치 push = 즉시 배포. **main 커밋은 배포되지 않는다.**
- 배포가 "Deployment failed, try again later"로 실패하면 GitHub 일시 오류 — 빈 커밋으로 재트리거(코드 원인 아님).
- 루트 `.nojekyll` 유지. 레포 전체가 공개 서빙됨 — 비공개 자료·개인정보(고객 실명·개인 연락처)를 넣지 않는다.

**원칙**
- 시공사례·거래처에 실제 상호 금지 → 전부 ○○ 익명(확인된 실적만 실명). 허위 실적은 법적 리스크.
- 플레이스홀더(전화 1600-0000 · ○○ 거래처 로고 · HERO/PHOTO 이미지 자리)는 고객 실제 자료로만 교체. 임시값 지어내기 금지.
- 커밋 전 로컬 HTTP 서빙(`python3 -m http.server`)으로 `/`·`/corporate/`·`/editorial/` 3경로 렌더 확인. `editorial`은 SRI 때문에 file://로는 안 열린다.

**절대 건드리지 말 것**
- `editorial/vendor/react*.min.js` — `support.js`에 SRI 해시가 박혀 있어 바이트가 바뀌면 React 로드 실패 → 페이지 백지.
- `editorial/support.js` — 렌더링 런타임.
- 실적 카운터 초기 state는 최종값(500/8/6/100) — 0으로 되돌리지 말 것.
- 각 사이트 파비콘은 identity(허브=속빈 다이아몬드, corporate=블루 R, editorial=브론즈 다이아몬드) — 임의 교체 금지.

**반복해서 틀렸던 것**
<!-- Claude가 실수할 때마다 여기에 한 줄씩 추가 -->
- 실적 카운터를 애니메이션 중간 프레임 스크린샷으로 보고 "0/잘못된 값"으로 판단했음. 확인은 애니메이션 완료 후 값으로.
